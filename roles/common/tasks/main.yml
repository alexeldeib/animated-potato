- name: Installs base packages
  apt:
    name: "{{ packages }}"
    state: latest
    update_cache: yes
  vars:
    packages:
    - apt-transport-https
    - curl
    - git

- name: install docker
  shell: snap install docker --classic

- name: install kubectl
  shell: snap install kubectl 
